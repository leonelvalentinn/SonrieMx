---
interface Props {
  name: string
  imgSrc: string
}

const { name, imgSrc } = Astro.props
---

<article class="w-full rounded-[19px] bg-white px-4 relative">
  <img src={imgSrc} alt={`Fotografía del Dr. ${name}`} class="w-full object-cover aspect-square drop-shadow-lg">
  <div class="w-10/12 px-4 py-2 text-center bg-slate-900 rounded-xl absolute bottom-4 right-0 -translate-x-[10%]">
    <span>{name}</span>
  </div>
</article>