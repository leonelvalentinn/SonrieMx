---
interface Props {
  name: string
  imgSrc: string
  specialty: string
  className?: string
}

const { name, imgSrc, className, specialty } = Astro.props
---

<article class={`w-full rounded-[19px] bg-white px-4 relative transition ${className}`}>
  <img src={imgSrc} loading="lazy" alt={`Fotografía del Dr. ${name}`} class="w-full object-cover aspect-square drop-shadow-lg">
  <div class="w-10/12 grid px-4 py-2 text-center bg-slate-900 rounded-xl absolute bottom-4 right-0 -translate-x-[10%]">
    <span>{name}</span>
    <small class="lg:text-nowrap text-xs">{specialty}</small>
  </div>
</article>